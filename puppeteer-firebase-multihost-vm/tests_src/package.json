{
  "name": "tests_host",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest -u --ci --reporters=default --reporters=jest-junit",
    "int_test": "mocha --recursive integration_tests",
    "e2e_test": "jest --ci --reporters=default --reporters=jest-junit",
    "vis_test": "mocha --recursive visual_regression_tests",
    "jest_dev": "jest --watchAll e2e_tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-admin": "^10.0.1",
    "jest": "^27.4.7",
    "jest-image-snapshot": "^4.5.1",
    "jest-junit": "^13.0.0",
    "mocha": "^9.1.3",
    "puppeteer": "^13.0.1"
  }
}
